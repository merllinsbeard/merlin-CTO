# Work order: #<N> — <короткое название тикета>

## Репозиторий и рабочее пространство

- Worktree: `<абсолютный путь>` (ветка `<ветка>`). **Никаких операций с git** (ни commit, ни push, ни stash, ни checkout).
- Канон: `CONTEXT.md`, спека #<spec>, тело тикета #<N>. Поверх этой ветки закрыты #<закрытые> — их решения не переписывать.

## Цель (из тела тикета)

<дословно или близко к тексту «What to build» из тикета>

## Инварианты

- Не трогать: <перечень закрытых решений и запретных зон — имена/копирайт, роуты, ACL, атомарная публикация и т.д.>.
- Существующие компоненты и токены — только использовать и расширять; новых рантайм-зависимостей нет.
- Тесты не подгонять; закреплённое старой строкой поведение переворачивать осознанно с объяснением в ответе.
- Не выдумывать результаты прогонов; если окружение не поднялось — честно сообщить.

## Шаги и критерии готовности

1. **<шаг>.** <что сделать>. Готово когда <проверяемый критерий: команда, файл, наблюдаемое поведение>.
2. **<шаг>.** … Готово когда …
3. <…>
N. **Прогон (двойной):** `<гейт репо>` дважды подряд — оба зелёные; `<линт/формат>` чистый; визуальные баселайны пересняты только там, где требует изменение.
N+1. В ответе: список файлов по пунктам, хвосты всех прогонов, что переснято и почему, что не сделано и почему.

## Визуальный барьер

Снимки — через существующий механизм `capture_evidence` (desktop и mobile вьюпорты репо), реальные состояния, не случайный CSS. Перечислить, какие новые снимки нужны тикету.
